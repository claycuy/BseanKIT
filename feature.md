# Feature

**Conditional**\
step(if)\
stepout(else)\ 

**Variable**\
setLet(let)\
setVar(var)\
setConst(const)\

**User interaction**\
setKey(keyboard)\
setMouse(mouse)\
layarToast(alert)\
consoleToast(console.log)\
makeToast(prompt)\

**Page navigation**\
moveToPage(href)\

# Exclusive Features

**Display mode**\
dalmode(dark and light mode)
